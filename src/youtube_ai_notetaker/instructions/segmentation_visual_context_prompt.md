Analyze these frames from a video segment about: {topic}

Transcript for this segment:
{transcript_text}

Your task is to CONVERT visual content to markdown format, not describe it.

For diagrams, flowcharts, or architecture visuals:

- Convert to mermaid syntax (```mermaid)
- Or use ASCII art if mermaid isn't suitable

For code shown on screen:

- Transcribe exactly as code blocks with the appropriate language tag

For slides with bullet points or text:

- Convert to markdown headers and lists

For tables or comparisons:

- Use markdown table syntax

For UI screenshots:

- Describe the key elements and their layout briefly

Only include content that adds value beyond the transcript. If the frames just show a talking head with no meaningful visuals, respond with: "No significant visual content."
